{
  "name": "nexora-crm",
  "version": "1.0.0",
  "description": "NEXORA Enterprise Sales CRM with Omni-channel Pipeline, WhatsApp Reports, and Real-time Telecalling",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install && npm --prefix backend run build",
    "start": "node backend/dist/server.js",
    "dev": "npm --prefix backend run dev"
  },
  "keywords": [
    "crm",
    "sales",
    "telecalling",
    "whatsapp-reports",
    "enterprise-crm"
  ],
  "author": "Akash Neelgund",
  "license": "MIT"
}
